<?php
    /*Existen varias formas incluir contenido en la página web a partir del resultado de la
    código PHP.
    1. Usando echo, que no devuelve nada (void), y genera como salida el texto de los parámetros que recibe.
        void echo (string $arg1, ...);
        
    2. Usando print, que funciona de forma similar, La diferencia más importante entre print y 
    echo, es que print sólo puede recibir un parámetro y devuelve siempre 1.
        int print (string $arg);
        
    Tanto print como echo no son realmente funciones, por lo que no es obligatorio que ponga parentesis
    cuando las utiliceis. Por ejemplo, el código del siguiente documento puede hacer igualmente utilizando echo
    */
?>